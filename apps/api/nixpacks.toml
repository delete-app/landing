# Force Python provider
providers = ["python"]

[phases.setup]
nixPkgs = ["python312Full"]

[phases.install]
cmds = ["python -m ensurepip", "python -m pip install -r requirements.txt"]

[start]
cmd = "cd src && python -m uvicorn app.main:app --host 0.0.0.0 --port ${PORT:-8000}"
